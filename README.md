# c-password-login

A simple C program that implements a masked password login system using file handling.

## Features
- Hidden password input using `getch()`
- Reads password from a file
- Login validation
- Console-based program

## How to Run
```bash
gcc password_login.c -o login
./login
# c-password-login

C password authentication system with masked input.
